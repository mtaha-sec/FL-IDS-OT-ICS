# ─── Core ML / FL ─────────────────────────────────────────────────────────────
torch>=2.0.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# ─── Federated Learning framework ─────────────────────────────────────────────
flwr>=1.5.0

# ─── Homomorphic Encryption ────────────────────────────────────────────────────
# TenSEAL implements CKKS (approximate arithmetic on floats) and BFV
# (exact arithmetic on integers) using the Microsoft SEAL backend.
# CKKS is used here because model weights are floating-point.
tenseal>=0.3.14

# ─── Preprocessing / utilities ────────────────────────────────────────────────
tqdm>=4.65.0
